env:
  NODE_ENV: staging
  AWS_REGION: us-east-1
  APP_NAME: predictive-maintenance-alerts

  DATABASE: "smart_factory_cohere"

envSecrets:
  MONGODB_CONNECTION_STRING: predictive-maintenance

# Kanopy security and ownership (requires web-app chart v4.30.0+)
security:
  dataClassification: "Internal"
  dataDesignation: ["MI"]

ownership:
  driEmail: javier.guajardo@mongodb.com
